apiVersion: v1
kind: Pod
metadata:
  name: lab-cluster-1
  namespace: cnpg-lab
spec:
  containers:
  - name: postgres
    readinessProbe:
      exec:
        command:
        - /bin/false
      initialDelaySeconds: 1
      periodSeconds: 5