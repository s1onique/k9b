# BAD FIXTURE: Shared PVC Deployment without required pod affinity
# This fixture should FAIL the verifier because:
# 1. It mounts a PVC that is shared with another deployment
# 2. It lacks required podAffinity.requiredDuringSchedulingIgnoredDuringExecution
#
# Expected: FAIL - Missing required pod affinity
apiVersion: apps/v1
kind: Deployment
metadata:
  name: k9b-scheduler
  labels:
    app: k9b
    fixture-type: bad
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: k9b
      app.kubernetes.io/component: scheduler
  template:
    metadata:
      labels:
        app.kubernetes.io/name: k9b
        app.kubernetes.io/component: scheduler
    spec:
      containers:
        - name: scheduler
          image: k9b:latest
      volumes:
        - name: runs
          persistentVolumeClaim:
            claimName: k9b-runs
      # NOTE: No affinity specified - this will cause Multi-Attach errors
      # when scheduler schedules on a different node than backend
