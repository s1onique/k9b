/**
 * Demo Shell Component Re-export
 *
 * Re-exports the main DemoShell component from the parent directory
 * to maintain module resolution compatibility.
 */

export { DemoShell } from "../DemoShell";
export type { DemoShellProps, DemoStep, DemoFinding } from "./DemoShellTypes";